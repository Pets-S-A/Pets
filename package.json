{
    "scripts": {
        "postinstall": "cd server && npm install",
        "start": "cd server && npm start",
        "test": "cd server && mocha --timeout 10000 --exit && eslint ."
    },
    "devDependencies": {
        "husky": "^4.2.5",
        "mocha": "^7.1.1"
    },
    "husky": {
        "hooks": {
            "pre-commit": "npm test",
            "pre-push": "npm test",
            "...": "..."
        }
    },
    "dependencies": {}
}
