{
    "scripts": {
        "postinstall": "cd server && npm install",
        "start": "cd server && npm start",
        "test": "cd server && mocha --timeout 10000 --exit"
    },
    "devDependencies": {
        "pre-commit": "^1.2.2",
        "mocha": "^7.1.1"
    },
    "pre-commit": [
        "test"
    ]
}
