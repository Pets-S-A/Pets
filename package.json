{
    "scripts": {
        "postinstall": "cd server && npm install",
        "start": "cd server && npm start",
        "test": "cd server && mocha --timeout 10000 --exit",
        "lint": "cd server && eslint ."
    },
    "devDependencies": {
        "mocha": "^7.1.1",
        "pre-commit": "^1.2.2"
    },
    "pre-commit": [
        "test",
        "lint"
    ],
    "dependencies": {}
}
